
  <div class="wpp">
    <style>
        th {
            vertical-align: bottom;
        }
    </style>

    <h2><%= _('Insights for ') %> <%= current_user %></h2>

    <span id="dash-tabs" class="tabgroup">
      <ul>
        <li class="killround"><%= _('Recurring Gift Recommendations') %></li>
      </ul>
      <div id="insights-increase"><%= render partial: "recurring_recommendations" %></div>
    </span>
  </div>
