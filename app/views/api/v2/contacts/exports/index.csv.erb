"<%== spreadsheet_header_titles_joined %>"
<%== CSV.generate do |csv|
  # Handles the assignments in contacts_controller#index
  contacts ||= @contacts
  contacts.each do |contact|
    row = spreadsheet_values(contact)
    csv << row unless row.empty?
  end
end %>
