"<%== spreadsheet_header_titles.map {|h| _(h)}.join('","') %>"
<%== CSV.generate do |csv|
  # Handles the assignments in contacts_controller#index and account_list#physical_newsletter_csv
  contacts ||= @contacts
  contacts.each do |contact|
    csv << spreadsheet_values(contact)
  end
end %>
