<% if current_user.setup_mode? %>
  $('#org_connection_box').html('<%= j(spinner(visible: true)) %>');
  document.location = '<%= setup_path(:org_accounts) %>';
<% else %>
  document.location = '<%= accounts_path %>'
<% end %>
